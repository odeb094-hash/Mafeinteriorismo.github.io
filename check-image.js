import fs from 'fs';

// simple node script to read the first few bytes of the jpeg or we can just serve it over a local express and then we can't really read it.
console.log("We need to install jimp");
